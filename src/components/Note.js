import React from 'react';

function Note(props) {
    // debugger

    // let note = props.note

    return(
        <div>
            {/* {note ? props.note : null} */}
            Hi Note!
        </div>
    )
}

export default Note